"""Benchmark suite -- measures real, reproducible performance numbers for
every subsystem that can be meaningfully measured without physical
camera/wristband hardware or a GPU (neither is available in this
environment -- see ``run_benchmarks.main()``'s report for exactly what
that excludes and why, rather than silently omitting it).

Run via ``python -m irix.benchmark.run_benchmarks``.
"""
